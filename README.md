# FLEXIBLE AI — All-in-one package

1. Set OPENAI_API_KEY in Render environment variables.
2. In Firebase console enable Google sign-in and add your Render URL to authorized domains.
3. Deploy on Render using render.yaml or manual settings.
